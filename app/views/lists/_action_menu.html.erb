<div data-controller="menu" class="relative">
  <button
    data-action="click->menu#toggle"
    class="absolute right-2 top-2 p-1 hover:bg-gray-100 rounded transition-colors z-20"
    aria-label="メニューを開く"
  >
    <svg class="w-5 h-5 text-gray-600" fill="currentColor" viewBox="0 0 20 20">
      <path d="M10 6a2 2 0 110-4 2 2 0 010 4zM10 12a2 2 0 110-4 2 2 0 010 4zM10 18a2 2 0 110-4 2 2 0 010 4z"></path>
    </svg>
  </button>

  <div
    data-menu-target="menu"
    class="menu absolute right-2 top-8 bg-white shadow rounded p-2 text-sm hidden z-30 w-28"
  >
    <a class="block px-2 py-1 hover:bg-gray-100 text-left"
        href="<%= edit_list_path(list) %>"
        data-turbo-frame="_top">編集
    </a>

    <%= button_to "コピー",
          reuse_list_path(list),
          method: :post,
          data: { turbo_frame: "_top" },
          class: "block w-full text-left px-2 py-1 hover:bg-gray-100" %>

    <% if current_user.can_copy? %>
      <%= button_to "テンプレ化",
            to_templates_list_path(list),
            method: :post,
            data: { turbo_frame: "_top" },
            class: "block w-full text-left px-2 py-1 hover:bg-gray-100" %>
    <% end %>

    <div class="border-t border-gray-200 my-1"></div>

    <%= button_to "削除",
        list_path(list),
        method: :delete,
        data: {
          turbo_confirm: "本当に削除しますか?"
        },
        class: "block w-full text-left px-2 py-1 hover:bg-gray-100 text-red-600" %>
  </div>
</div>
