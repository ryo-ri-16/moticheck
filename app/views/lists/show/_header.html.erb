<% content_for(:back_path) { @back_path } %>
<% content_for :title do %>
  <div class="flex-1 px-2">
    <div class="flex flex-col items-center gap-1">
      <h1 class="text-lg font-semibold text-gray-900 truncate"><%= @list.title %></h1>
    </div>
  </div>
<% end %>
